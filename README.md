# TJA Client Portal — Sandbox

A front-end-only prototype of The James Agency client portal, built to present
the concept internally. **No backend, no real auth, no data wiring** — every
number is placeholder/sample data for now.

## Run it locally

From this folder:

```bash
python3 -m http.server 8080
```

Then open **http://localhost:8080** in your browser.

## Demo logins (two roles)

| Role            | Email                                | Password         | Can do                                                              |
| --------------- | ------------------------------------ | ---------------- | ------------------------------------------------------------------ |
| **Client**      | `celticelevator@thejamesagency.com`  | `celticelevator` | Read-only data tabs; upload Files; comment/draw/approve Present Docs |
| **Admin (TJA)** | `clientservices@thejamesagency.com`  | `admin`          | Everything — upload deliverables/files, manage versions, rename     |

Admin sees a **Mode** switch (Admin / Client view) in the top bar to preview the
client experience without logging out. **Clients never see this switch.**

> ⚠️ The login is a **client-side demo gate only** — it does not secure anything.
> Real authentication comes in a later phase.

## Pages (V1 — per the 06/16 workshop)

| Tab                   | Purpose                                                            |
| --------------------- | ----------------------------------------------------------------- |
| **Executive Summary** | The home page — burn, condition, service lines, milestones, to-dos, dependencies, KPIs, PR coverage. All editable by admin. |
| Project Plan          | Full plan w/ line items (replaces "Key Dates")                    |
| Status                | Service-line detail (target for the SAP + Status merge)          |
| Present Docs          | Creative review — upload, version, draw, pin comments, approve   |
| Files                 | Signed agreements + shared docs (both roles upload)              |
| Backlog               | Retainer-only — SOW conversation starters                        |

A **Retainer / Project** toggle at the top switches between engagement homepages
(burn speedometer for retainers, Domino's-style pizza tracker for projects).

## Swapping in real data

All content lives in **`assets/data/celtic-elevator.js`**. Edit that one file —
nothing else needs to change.

## Brand

Colors/fonts in `assets/css/styles.css` are **placeholders** (inferred orange
`#FF7800` + Inter). Drop in the official TJA brand guide and we re-skin via the
CSS tokens at the top of that file.

## Version log

| Version | Notes                                                                    |
| ------- | ------------------------------------------------------------------------ |
| v1.99   | **Projects: editable tracker + per-task internal/client toggle, hours removed.** Three changes to every **not-yet-completed** project (completed projects are left exactly as they are): (1) the allocated-hours line is gone from the Project Progress tile — tasks are the only imported data shown; (2) each task row has an admin toggle (🔒 Internal / 👁 Client) that flips its visibility, persisted per task, and anything named "Client Communications" is now internal by default; (3) the pizza tracker is manual and admin-editable — every open project seeds with 3 empty steps, admins rename each step in place, click a dot to mark it complete, ✕ removes a step, and "＋ Add step" adds more. An admin-managed tracker is sacrosanct — never overwritten on re-sync, even after the project completes. Verified across 20 open + 33 completed WMJ projects; client view hides all internal tasks and every admin control. |
| v1.98   | **All clients heal to exact active-month hours; stale ÷12 estimates never render.** Root cause the other 19 clients stayed on 54-style estimates: the dashboard only refreshes the client you open, and each browser still cached ÷12 values from before the feed redeploy. Now a stored SOW value is only used if it carries the active-month flag (retainerValueMonthly) — a leftover ÷12 estimate is ignored, so a client shows a brief "—" then the exact figure, but never a wrong number. Verified: SkyViews with a stale 118 renders "—" instantly, self-heals to 72.22. Opening the Clients page bulk-refreshes every client's value in one visit (confirmed all 23 update). |
| v1.97   | **Removed the SOW description from the burn tile.** The orange hint text under the burn readout is gone (admin mirrors client). Note: the "ESTIMATED / 54 hrs" Cameron saw after v1.96 was stale browser-cached v1.95 assets + the previously-stored estimate — the live site was already correct, and the stored value self-heals to the exact active-month figure within ~20 seconds of loading current assets. |
| v1.96   | **Exact active-month retainer hours are live.** Cameron redeployed the feed with the month-column fix (new /exec URL wired in): 31 of 33 retainer SOWs now return month-by-month billing, so every client's burn total = **this month's actual billing ÷ rate** — no more ÷12 estimates. DNA now reads 108.05 hrs (was a 54 annual-average estimate); flighted/uneven retainers correct themselves (AZDCS 4.1→49.2, SanTan 6.7→80, SkyViews 118→72.2), and clients with no annual total but active July billing light up for the first time (FIRRP 10, PPS 27.8, Sub-Zero 20 hrs). The labeled ESTIMATED transition path is now dormant. |
| v1.95   | **Active-month retainer hours (no more ÷12) + the curated Slack PR-wins button.** Retainer hours now come from the **current month's billing ÷ rate only** — flighted/partial-year retainers are never annual-averaged, and an off-month correctly shows no retainer. (A clearly-labeled "ESTIMATED: annual ÷ 12" transition path survives only until the sheet script redeploy delivers month columns, then dies automatically.) New **→ Slack** button on each PR hit (admin only): the team picks which wins go to the channel — nothing auto-sends. Clicking posts the hit (client, outlet, date, link, impressions, AVE) through a private Apps Script proxy that holds the webhook (never in this public repo) and flips to a persistent "✓ Sent" so nobody double-posts. Buttons stay hidden until the proxy URL is pasted into `slack-wins.js`. |
| v1.93–94 | **Fix: SOW burn totals now load on the client dashboard itself (Donor Network "—" bug).** The retainer-value feed only ran in the Clients-page poll, so opening a client's dashboard directly never received its SOW total — burn showed "—" despite signed $ in the sheet (same bug shape as the v1.76 PR fix). The dashboard now fetches the feed for the current client on load (first-ever load takes a few seconds — Apps Script cold start — then the value persists so later loads are instant). Also: the hours math now **prefers the current month's actual billing ÷ rate** when the feed provides month columns (exact for partial-year retainers like DNA's Jul–Dec schedule), falling back to total ÷ rate ÷ 12 — pending a one-paste Apps Script update on Cameron's account to populate the month columns (the sheet's date-formatted headers defeat the current script's text matcher). |
| v1.92   | **Burn now works for every SOW-matched client.** v1.91 stored each client's SOW-derived hours (signed retainer $ ÷ rate ÷ 12) but only displayed them as a hint — so every client except A New Leaf (hand-entered) showed a "—" burn. The total now **falls back to the SOW figure automatically** when no discipline hours are entered: 20 clients get a live burn speedometer immediately (e.g. AZ Humane 3.94 of 12 hrs = 33%). Hand-entered discipline hours still take precedence (A New Leaf unchanged), and the per-discipline **split stays manual** — service rows stay neutral with an admin note ("total ~N hrs/mo comes from the signed SOW — split it here") until hours are entered. Clients just see the working %. Clients with no signed $ in the sheet (FIRRP, PPS, Sub-Zero…) correctly stay unset. |
| v1.91   | **Retainer-value SOW feed wired in — advisory only, never guesses a split.** New read-only Apps Script feed (revenue-forecasting workbook → signed retainer $ ÷ hourly rate) matched against 20 clients on the current roster. This does NOT auto-fill contracted hours or disciplines (the feed has no per-discipline breakdown, so guessing one could show a client a fabricated split) — it sets an admin-only advisory line in the burn tile: "SOW suggests ~N hrs/mo," visible only while a client's contracted hours are still unset, and flags any SOW still pending signature. Admin still enters the real total + per-discipline split by hand, same as A New Leaf. |
| v1.90   | **Project archive/curation, live project counts, and a smarter engagement-mode default.** Admins can now **archive** individual projects (📁 button, mirrors the Clients page pattern) — archived projects are hidden from the client entirely but stay in an admin-only "Archived" tab on the Projects folder, ready to restore. This is the interim fix for "small tasks rolled into a retainer shouldn't show up as a project tile" while we wait on a real WMJ Project-Type field. Client tiles on the Clients page now show a **live** "Monthly Services · N projects" count (computed from real dashboard state) instead of a static string stamped at creation. Fixed the engagement-mode default: opening a client for the first time in a session now lands on whichever engagement (Monthly Services vs Projects) actually has data, instead of always defaulting to Monthly Services — and the mode is now scoped **per client**, so an admin's last-used mode on one client no longer bleeds into the next client opened in the same tab. |
| v1.89   | **Tiles fill the screen vertically — no more dead space at the bottom.** After the width-based scaling, a vertical fill factor now stretches tile heights + row positions (only — widths, fonts, columns, and order untouched) so the bottom row lands right at the bottom of the viewport on every screen (~20px breathing room, no page scroll). Shorter screens compress the boxes (content scrolls inside tiles); taller screens lengthen them (PR shows more hits, etc.). Verified at 1440×900, 1920×1080, and 2000×1280 on both Monthly Services and Projects. |
| v1.88   | **Dashboard scales proportionally to every screen size.** The tile layout is now treated as a fixed design canvas (built at ~1633px) and the whole thing — tiles, gaps, and text together — scales uniformly to exactly fill the content width, like a slide deck: same layout, same spacing, same ratios on every monitor (smaller laptops scale down, ultrawides scale up; phones keep the stacked single-column mode). Re-fits live on window resize (ResizeObserver + a setTimeout debounce — the old requestAnimationFrame throttle never fires in background tabs, which silently killed re-fits). Verified at 1440 / 1920 / 2560 / 390px: tile ratios identical to the design at all sizes, right edge always flush with the North Star banner. |
| v1.86   | **Cameron's final layouts baked in + locked for good; tab-switch resize bug fixed.** The Monthly Services and Project tile arrangements he built with the v1.84 editing window are now hard-coded as the permanent layouts (stored layouts ignored; stretch-to-fill keeps the right edge on the North Star's end at any screen size). All layout controls removed again — no Copy/Lock/Reset-layout, drag, resize, or remove; code-only changes on request. Root-caused and fixed the "tiles change size after visiting Status" bug: a document-wide mouseup handler persisted tile sizes even while the Exec page was hidden (offsetWidth reads 0 on a hidden page), writing zero-widths into the layout; it now only runs while the page is visible and never persists collapsed reads, and fitCanvas bails when the page is hidden. |
| v1.85   | **Removed the orange "drag the dial" hint in admin view.** The burn tile now reads the same in admin as in client view (just the % + hours), so tiles can be resized/arranged without the extra line shifting things. |
| v1.84   | **Burn % beside the speedometer + one-time layout editing window.** The big % readout now sits to the right of the gauge (gauge slimmed to 180px, no wrapping). Layout editing is temporarily back — drag, resize, remove/restore tiles, Reset layout, Lock layout — so Cameron can do one final arrangement. New **📋 Copy layout** button copies the page's exact tile JSON to the clipboard; paste it to Claude and that arrangement gets baked into code permanently, then editing is removed for good. While unlocked, tiles use raw coordinates (the stretch-to-fill from v1.83 applies only when locked, so what you drag is exactly what saves). |
| v1.83   | **Grid stretches to meet the North Star's end on every screen.** Root cause of the recurring layout pain: the tile grid was a fixed 1112px block that only shrank, never grew — fine on a ~1280px window (where it was verified), but on a wide monitor it left a dead gap and the Milestones column ended far short of the North Star banner. Columns now stretch horizontally to fill the content width (x/width scale; heights, font sizes, and v1.80 order unchanged), so the third column's right edge lands exactly on the banner's end at any window size — retainer and project alike. Also discovered the Supabase `app_state` table is empty (mock logins never authenticate, so cross-device writes silently fail) — flagged for the backend phase. |
| v1.82   | **Layout restored to the exact v1.80 geometry — still fully locked.** v1.81's lockdown was right but it also redesigned the tile positions/sizes, which was not asked for. Reverted the geometry byte-for-byte to v1.80 (burn 525 / service 474 / milestones 284 / todos 186 / dependencies 176 / kpis 185 / pr 558, original positions) for retainers and projects alike. The lockdown stays: no layout controls, no drag/resize/add/remove, stored layouts ignored — changes happen in code only, on request. |
| v1.81   | **Layout is now fixed, locked, and evenly gridded (retainer + project).** Removed all layout editing — no Lock/Reset-layout buttons, no tile drag, resize, add, or remove. `getLayout` always returns the hard-coded layout and ignores any stored/Supabase layout, so it can't drift or be changed except in code. Re-laid the tiles into a clean 3-column grid with aligned rows and equal 16px gaps (retainer: burn + service tall, milestones+todos stacked, then pr/dependencies/kpis as an even bottom row; projects: same minus PR/KPIs). "Reset to actuals" (a data control, not layout) is unaffected. |
| v1.80   | **Fix: reverted the retainer layout propagation that broke sizing.** v1.77 pointed every client's retainer layout at A New Leaf's arrangement, which resized/misaligned tiles across clients (and hid the Unallocated line). Reverted — all retainers re-adopt the standard `DEFAULT_RETAINER_FREE` layout (version bumped so it re-applies everywhere). |
| v1.79   | **Unallocated hours are now clickable (admin) → project drill-down.** Clicking the "Unallocated" line opens a popup listing the **project names + billable hours** behind those out-of-discipline hours (e.g. A New Leaf's two website builds = 0.75 + 0.38 hrs). The retainer transform now carries per-department project breakdowns to power it. |
| v1.78   | **Burn counts ALL billable hours + an "Unallocated" flag; cleaner unconfigured state.** The burn now includes billable hours in WMJ departments that don't match any discipline (so it reflects *all* billable, not just what the disciplines capture). Those leftover hours are surfaced as an **admin-only "Unallocated" line** (shown as "in burn") so the PM team can spin up a discipline to categorize them — they are NOT dumped into an existing discipline. Clients don't see it. A retainer with **no contracted hours yet** now shows a clean "—" burn + "set contracted hours" note and neutral "—" disciplines (instead of everything going red). |
| v1.77   | **Retainer burn ↔ disciplines linked + distribution popup; layout propagation; polish.** The burn is now the **sum of the disciplines' used hours**, so dragging a service-line slider updates the burn live (linked). Changing the total burn (drag the dial or edit the %) opens a **popup** asking which disciplines should absorb the change — split evenly across any you check (all / several / one). **Reset to actuals** moved up next to Lock layout (clears every manual % adjustment). Every client's retainer layout now **adopts A New Leaf's arrangement** (reference layout + version bump). Contracted-hours arrows step by **1** (decimals still allowed), and the decimal box no longer clips. |
| v1.76   | **Retainer: draggable % sliders + burn override + Reset to actuals; PR loads on the dashboard; decimal fix.** Re-added admin **drag sliders** on each service-line bar (override the shown utilization %) and made the **burn dial draggable** to override the shown total % — the real hours/percent stay in the caption as ground truth, clients see the adjusted %. New **↺ Reset to actuals** button clears all manual overrides. The dashboard now **pulls PR coverage directly on load** (previously it only refreshed via the Clients page, so opening a client dashboard showed empty PR). Widened the contracted-hours input so decimals (e.g. Creative 34.63) aren't clipped. Overrides persist and survive the WMJ re-sync. |
| v1.75   | **Critical fix: bulk-sync writes were being dropped (data "disappeared").** The Supabase push debounce was keyed by scope only, so a sync pushing ~40 clients under the same "dashboard" scope in quick succession cleared each previous client's timer — **only the last client processed was ever saved to Supabase**; the rest (incl. A New Leaf's seeded service disciplines) were silently dropped, then the stale Supabase copy overwrote good local data on next load. Now keyed by **client+scope** so every client persists. Added a **self-heal** on dashboard load: a retainer with missing service disciplines re-seeds from its template (never a blank Service Lines tile) and saves back. This is also why PR looked empty on older builds. |
| v1.74   | **PR coverage pulled from the team's Google Sheets (live).** PR hits are maintained by the team in one Google workbook per client; the portal now reads them (new `client-pr-sheets.js` registry → `syncPR()` on the hourly poll) and mirrors them into the Executive Summary's PR Coverage tile — outlet, date, impressions, ad-value equivalent, and a link to the coverage, newest first, with a "N hits YTD" count. Sheet-driven clients are read-only (no manual Add hit). A New Leaf is wired; add other clients by publishing their PR workbook and registering its id + gid. |
| v1.73   | **Fix: retainer service lines no longer vanish + A New Leaf total corrected.** The Service Lines tile was gated on WMJ actuals being present, so a client whose timesheet hadn't synced (or was cleared) showed a blank tile even though the contracted plan existed. Retainers now always render the disciplines view (missing actuals just show 0%, never a blank tile). A New Leaf template total corrected to **101.63 hrs** (Creative buffered 33 → 34.63); a guarded one-time sync correction bumps the existing seed without touching manual edits. |
| v1.72   | **Client code (from Workamajig) replaces initials.** Each client now carries a **client code** — the leading token of its WMJ `Campaign_Name` (e.g. `ANL 2026 Retainer` → **ANL**; A New Leaf, RCS, HVH, AZDCS…). The WMJ sync reads it and stores it per client, so it's set automatically for every WMJ client; the avatar/tile shows the code (falling back to name-initials only if there's no code and no logo). The add/edit-client modal's field is now **Client code** (auto-filled, editable). Codes match Workamajig exactly so a human can cross-reference. |
| v1.71   | **Retainers: admin-set contracted hours + auto status (the plan model).** There's no WMJ "plan" sheet, so contracted hours are entered by the admin: a monthly budget **per service discipline** (PR / Strategic Oversight / Creative / Paid Media by default), and the **total contracted = their sum** (the burn denominator). Actual hours worked still come from the WMJ timesheet (matched to a discipline by name/department) and track against the budget. Each discipline's **% of retainer** = its hours ÷ total (auto-updates when you shift or add hours), and its bar = actual ÷ contracted. All fields stay **editable in the workspace** (admin). Auto **status chip**: 0 hrs = Not started, >0 = In progress, ≥100% = Completed, and past 120% the chip turns **vibrant red "Over"** (no extra button). New-client setup now collects these contracted hours up front. A New Leaf is seeded as the template (100 hrs: PR 30 / Oversight 31 / Creative 33 / Paid Media 6). Retired the v1.69 drag handle (contracted-hours editing replaces it). |
| v1.70   | **To-Do colours: Client auto-matches the logo; TJA always orange.** The "Client" to-do tag now defaults to a colour sampled from the client's actual logo (dominant brand colour, precomputed per client in `client-logos.js` — e.g. A New Leaf = leaf-green). Admins can still override it with the colour picker (the override wins and persists). The "TJA" tag is now always TJA orange (`#F68E21`) instead of the muted brown it picked up in light mode. |
| v1.69   | **Retainer service lines: draggable admin handle + hours hidden from clients.** Each service-line bar gets a circular drag handle in admin view — drag it to set the shown utilization % for presentation (persists as a per-line override keyed by name, so it survives the hourly WMJ re-sync; double-click resets to actual). Admin caption shows the **real hours + real %** (`6.25 of 16 hrs used · 39%`) as ground truth; the client caption shows only the shown **%** (no hours). Clients never see the handle. |
| v1.68   | **Client view hides empty engagements + sync hardening.** In the client view, the top Monthly Services / Projects toggle now only shows engagements that actually have data — a project-only client can't see (or land on) an empty Monthly Services page, and vice-versa; when there's only one engagement the toggle hides entirely. Admins still see both toggles so they can set either up. Also made the WMJ sync **sequential** (projects → retainers) so the retainer write always lands last for clients in both sheets — a stale projects write can no longer clobber a client's `wmjServiceLines`. |
| v1.67   | **Retainer Monthly-Services page (v1, real data).** New retainer pipeline off the separate retainers sheet. Per client, **Service Lines = User_Department** each showing a **share of the retainer** (allocated ÷ total allocated, sums to 100%) and a **utilization bar** (actual billable ÷ allocated — turns red over 100%). The **burn speedometer** shows billable-of-allocated hours. `Allocated_Hours` is deduped per task (it repeats on every timesheet row); billable is summed. Projects + retainers sync separately and never mix. |
| v1.66   | **Project due date is a calendar picker (admin).** The Due Date in the Project Progress tile is now a native date-picker for admins (clients still see the formatted date). Converts between the stored "MMM D, YYYY" format and the picker's ISO value; changes persist. |
| v1.65   | **Projects polish + a universal rules config.** Projects: the top banner is now **"Goal"** (not North Star) and the **due date shows only in the Project Progress tile** (no duplicate). The project layout **template is now RCS** (`referenceProjectLayout`), cloned and **locked in** by default for every project (`PROJECT_LAYOUT_V` bumped so all existing projects re-adopt it). New central **`RULES`** block in `wmj-transform.js` — one place for universal project exclusions (`DROP_ROW` / `INTERNAL_TASK` / `EXCLUDE_PHASE`); added `EXCLUDE_PHASE` so oversight/management/comms items (e.g. "Client Communications + Project Oversight") never appear as pizza-tracker phases. The **Tasks tile fades** at the bottom so a partly-visible row softens instead of hard-cutting. |
| v1.64   | **Projects v1 test: real phases for single-workstream builds + projects sync re-enabled.** Website-type projects put all work under one WMJ Project_Name, so the tracker showed one dead dot. Now, when a campaign has ≤1 workstream, phases are derived from the tasks' **top-level numbering** (1,2,3…) — e.g. RCS / Saddleback website builds now show 5 real phases. Re-enabled the **projects-only** sync (retainers are a separate sheet + pipeline, not wired — the two never mix). |
| v1.63   | **Clean-slate reset.** Blanked Celtic's seed (removed all test/placeholder data); one-time browser purge wipes the auto-created client roster + every `tja_dashboard/files/deliverables_*` workspace; paused the old WMJ auto-sync (engine kept). Prep for re-implementing data from the revised retainer + project sheets. |
| v1.62   | **Fix: "All clients" hidden from client view + admin-gating audit.** The button was gated on `isAdmin()` (which stays true while an admin previews as a client), so it leaked into client view. Now gated on the **effective role** — hidden in client view (and for real clients), shown for admins. Audited the rest across Exec Summary, Present Docs and Projects: Lock/Reset layout, tile-remove, add buttons, sliders, and all `.admin-only` controls hide correctly in client view; the Admin/Client-view switch stays so admins can exit preview. |
| v1.61   | **Present Docs: upload + review timestamps and a revisions-due date.** Uploads now stamp **date + time** (not just date). Submitting a review records the **date + time it was reviewed** plus the status chosen — shown in the version meta line ("uploaded … · reviewed … (status)"). New editable **Revisions due** date field per version, persisted. (Automated review reminders off these dates are bookmarked for the backend phase.) |
| v1.60   | **Admin topbar: moved the "All clients" button.** It now sits to the right of the client logo/name/description (in a new .topbar-left group) instead of in the right control cluster; dropped the ← arrow and renamed it "All clients". Admin-only, unchanged behavior. |
| v1.59   | **Project hours readout is client-facing.** Reverted v1.58 for the pizza-tile "X of Y hrs allocated" readout — clients see it again (per-task hours stay admin-only). Confirmed the project **due date = project end** (latest task completion date). |
| v1.58   | **Projects: client-facing cleanup pass (review build).** Tracker capped at **8 phases** and the staggered graphic fixed (dots align, labels clamp to 2 lines). **Hours hidden from clients** (tasks + the allocated/budget readout) — admin-only. Expanded the hidden-task filter to also drop **proofing, kickoff questions, non-billable, and internal revisions** (client revisions stay). **Non-billable Project_Names** are now excluded too (e.g. a stray "DNA Non-Billable" phase). North Star left as a manual field (not hidden). |
| v1.56   | **Client logos auto-added.** Each client tile now shows its real website logo (via DuckDuckGo's icon service, by domain). New `client-logos.js` maps clients to their researched official domains; the WMJ sync sets/refreshes each client's logo (never overwriting an uploaded one). ~31 of 34 resolved; the few that couldn't be confidently identified (Ancara Hospitality, Ignite Adventure Park, The Asbury Collection) keep initials — upload a real logo via Edit client. |
| v1.54   | **Clients page fills full width.** Removed the 1120px max-width cap on the Clients page so the roster grid spans the whole page — the auto-fill grid simply fits more tiles per row (5+ at desktop width) without resizing them. |
| v1.53   | **All projects mirror the DNA Stratagem template + KPIs hidden on projects.** Every project's Executive Summary now clones the layout of the designated template project (`wmj_dnastratagem`, read live from local storage so it matches whatever it looks like in this browser), falling back to the monthly-services reference. **KPIs and PR Coverage are hidden on all projects.** A new project-layout version (`PROJECT_LAYOUT_V`) forces existing project layouts to re-adopt the template on load **without touching monthly-services layouts**; bump it again to re-propagate after re-arranging the template. |
| v1.52   | **Clients page: Current / Archived folders + auto-sync.** WMJ now **syncs automatically** on every Clients-page load and hourly while open — the prominent "Sync" button is gone, replaced by a quiet "Synced …·↻" (the ↻ just forces a manual refresh). *(True 24/7 sync independent of an open browser still needs the Supabase/Option-B backend.)* New **Current / Archived** tabs with live counts: **drag any client tile onto a tab** to file it (or use the 📁/↩ button on the tile). The `archived` flag persists per client (localStorage + Supabase roster) and doesn't stop WMJ from updating that client. |
| v1.51   | **Workamajig live project data (v1).** New `⟳ Sync Workamajig` on the Clients page pulls the published project CSV (client-side, CORS-open — Option A), transforms it, and writes each client's **projects**. Auto-creates any client in the sheet that doesn't exist yet (login = `name@thejamesagency.com` / `name`, lowercased), matching by normalized name. Per project: **pizza-tracker phases** = the sheet's Project_Names, **due date** = latest Plan_Completion_Date, **allocated/contracted hrs** (contracted parsed from the `(123)` campaign suffix), and a **Tasks** module (renames "Service Lines" on projects) grouped by workstream with discipline tag · hours · status. **Internal** tasks (Internal Revisions, Mech/Check, admin, huddles…) are hidden from clients, shown greyed to admins. Non-billable / "The James Agency" rows are excluded. Syncs hourly while the Clients page is open. New files: `wmj-transform.js` (pure, Node-testable), `wmj-sync.js`. Projects-only for now; Monthly Services stays manual. |
| v1.50   | **Project tiles now truly mirror Monthly Services dimensions.** v1.49 used a baked default; if a client's reference (Celtic) layout differed, projects didn't match. Projects now clone the **same reference layout as Monthly Services, minus PR** (`referenceProjectLayout`) — identical box sizes/positions. Applied on seed, default render, and Reset layout. |
| v1.49   | **Project Exec Summary: matched tile sizes + due-date on the tracker.** Projects now default to the SAME tile positions/sizes as monthly services (minus PR Coverage, which stays hidden on projects) — 3 columns, identical box dimensions. The project **due date** now appears prominently in the pizza-tracker tile's condition area as an orange "Due date" banner (editable by admins). Existing projects can adopt the new sizing with **↺ Reset layout**. |
| v1.48   | **New clients clone your actual Celtic layout + a Reset button.** Instead of hard-coded numbers, a new client now copies the reference client's (Celtic) **current** Exec Summary layout from this browser — exact positions, exact widths, and every box given an explicit height (derived from the column gaps; trailing tiles use a captured fallback) so blank boxes are the same SIZE as Celtic's, whatever arrangement Celtic is in. New admin **↺ Reset layout** button (next to Lock) re-applies that default to any engagement — use it to bring an existing/older client (created before this) in line with Celtic. `TJA_STORE.referenceRetainerLayout()` is the shared source. |
| v1.47   | **Default layout now matches Celtic's exact tile sizes.** v1.46 only matched the column order; tiles still auto-sized to content, so a blank client's boxes were shorter than Celtic's. The default retainer layout now bakes Celtic's exact box **position + width + height** (`DEFAULT_RETAINER_FREE`), so every fresh monthly-services workspace renders identical-sized tiles regardless of content. Admins can still drag/resize/lock; saved layouts untouched. |
| v1.46   | **Default Exec Summary layout = Celtic's monthly-services arrangement.** Fresh clients (and any engagement without a saved layout) now lay their tiles out in the curated 3-column template instead of a height-driven auto-pack — **left:** Burn/pizza then PR Coverage · **middle:** Service Lines then Dependencies · **right:** Milestones, To-Do's, KPIs. Laid out at design width and scaled to fit narrower windows (same as a saved 3-col layout); admins can still drag/resize and Lock it. Saved layouts (e.g. Celtic's) are untouched. |
| v1.45   | **Custom client logins.** The Add/Edit client modal now has an explicit **Client login** section — you type the **email + password** yourself instead of the app generating them. Validated (valid email format, 4+ char password, email must be unique across clients) and editable later via ✎ Edit (prefilled with the current login). The credentials authenticate through the mock login exactly as entered. |
| v1.44   | **Add new clients (admin).** The Clients landing page can now create real workspaces. **＋ Add client** opens a modal (name, auto-derived initials, optional logo upload, tagline, engagement type: Monthly Services / Project / Both); on submit it registers the client and seeds a **blank-but-structured** workspace, then shows an auto-generated sandbox login to hand off. Each tile has hover **✎ edit** / **🗑 delete** (delete wipes that client's data; built-in clients can't be deleted). Roster persists to `localStorage["tja_clients"]` and a Supabase `_registry/clients` scope. **Architecture:** the dashboard no longer hard-loads Celtic's data — a bootstrap resolves the selected client's own data (`CLIENT_SEEDS[id]` for built-ins, else the template factory) *before* `app.js`, so every client loads its own Exec Summary / Files / Present Docs. New files: `client-store.js`, `client-template.js`. Also fixed a divide-by-zero (`NaN%`) on the burn gauge when a new client has 0 contracted hours. |
| v1.43   | **Executive Summary: Lock layout.** New admin **🔓 Lock layout** toggle (top-right of the Exec Summary). Locking **snapshots every tile's exact position + size** and freezes them — drag handles, the resize grips, and the tile-remove ✕'s all switch off, and the "Hidden tiles" row is hidden. The lock flag persists per engagement, so the arrangement stays **exactly as set across refreshes and content edits** (sliders, statuses, to-dos etc. remain fully editable — only the layout is pinned). Toggle again to unlock and rearrange. |
| v1.42   | **Present Docs: typed signatures.** The approval signature pad now has **Type / Draw** tabs. *Type* (the default) renders the signer's name in a cursive script (Great Vibes) with a live preview as they type — prefilled with the client name; *Draw* keeps the original freehand pad. Either way the result is saved as a signature image on that version and flows into the "✓ Approved & signed by …" line and the exported PDF unchanged. |
| v1.41   | **Present Docs: PDF export + approval signature.** (1) **Export PDF** (button in the review modal + a quick-export ⬇ on each gallery card): renders a comprehensive PDF — the proof image with **drawings + numbered pins baked in** at full resolution, a header (deliverable name · **version** · **direction**: Approved / Approved w/ changes / Revisions · **export date**), the full **numbered comments** list, client + agency notes, and the **signature** sign-off. jsPDF is lazy-loaded from CDN on first export. (2) **Approval signature:** choosing *Approve* (or *Approve with changes*) and hitting **Submit Review** now opens a **signature pad** (draw, or type a name) — the signature, signer name and date are saved on that version, shown as "✓ Approved & signed by …", and included on the exported PDF. |
| v1.40   | **QA sweep: Exec Summary scales to fit narrower windows.** Found via a full sweep (all pages · both modes · light/dark · admin/client): the free-canvas exec layout is designed at ~1112px wide, so on windows narrower than ~1370px the right-hand tiles (Milestones/To-Do's/KPIs) overflowed and were cut off / forced into horizontal scroll. Now the tile canvas **scales to fit** any window ≥760px (full size at design width, shrinks just enough below it, stacks <760), preserving the arrangement and keeping drag/snap correct (deltas divided by scale). Everything else checked out clean (no clipping, overlaps, contrast, or console errors). |
| v1.39   | **Present Docs theme + fit, and steadier Condition row.** (1) The review modal's dark override is now scoped to just the image **stage** — the chrome + right review panel **follow light mode** (white in light theme; stage stays dark for proofing). (2) Tightened the right panel (smaller note boxes, gaps, status padding) so it **fits without the bottom being cut off**. (3) Exec Summary **Condition**: status text moved to the **left of the 3 colour dots**, which are now pinned right — so they no longer shift when the label changes ("On Track" ↔ "Needs Attention" ↔ "Off Track"). |
| v1.38   | **Present Docs image zoom + pan.** The proof image now zooms: **scroll to zoom toward the cursor**, ＋ / − / **Fit** controls (bottom-centre), 100%–500%. Image, markup canvas and comment pins scale together (drawing & pin placement stay aligned at any zoom). Pan with **Space-drag** or **middle-mouse-drag** when zoomed in. Resets to Fit on open / version switch / close. |
| v1.37   | **Reporting tab (placeholder).** Added a **Reporting** item to the left nav (between Present Docs and Files) with a coming-soon placeholder page — contents to be built out later. |
| v1.36   | **Present Docs: split notes + visible status options.** (1) Per-version notes are now split into **Client Notes** and **Agency (TJA) Notes** (two labelled, separately-saved fields; old single notes migrate into Client). (2) The status options (Approve / Approve with changes / Revisions needed) were invisible until selected — made the text **white** so all three are readable; the selected one still colours green/amber/red. |
| v1.35   | **Speedometer no longer clipped + tiles scroll instead of cutting off.** (1) The retainer burn gauge's `viewBox` was too short so the arc's bottom corners were cut off — shifted it (`0 25 260 180`) and updated the drag math so the full dial shows. (2) Tile content now **scrolls within the tile** (styled thin scrollbar, both roles) when it doesn't fit, instead of clipping text; removed the PR list's fixed inner max-height so it flows into the tile's single scroll. |
| v1.34   | **Fix: tiles wouldn't drag with a real mouse.** Resize worked (CSS) but dragging didn't, because a real mouse-drag on the header started a native text/element drag that suppressed the `pointermove` events. Fixed by capturing the pointer on the drag handle (`setPointerCapture`, events on the handle instead of `document`) and disabling text selection on it (`user-select:none; touch-action:none`). Applies to both Monthly Services and Projects exec views. |
| v1.33   | **Present Docs fix + in-image comment notes.** (1) **Fixed deliverables not opening** — the page re-attaches its click handlers every time its tab repaints (its DOM is rebuilt on engagement/client switches; the old one-time wiring left the rebuilt gallery/modal dead). Split `init()` into per-render element wiring + once-only document/window listeners. (2) Clicking a comment (or its pin) now pops the **note's text box up on the image, anchored to the pin** (editable, with a close button, two-way synced with the side panel) instead of only highlighting the side row. |
| v1.32   | **Free-floating tiles + admin client picker.** (1) The Executive Summary is now a **free canvas** — admin tiles are absolutely positioned and can be **dragged anywhere** (by the header) and **resized** in both directions, no longer constrained to columns. Dragging **snaps to align** with other tiles' edges/centres within ~9px (orange guide lines show the alignment). New/restored tiles auto-pack into a 3-column default; on small screens it falls back to a vertical stack. (2) Admins now land on a **Clients page** (`clients.html`) after sign-in instead of going straight into a client — pick a workspace to open it; a **← Clients** button in the dashboard top bar returns there. Client list lives in `assets/js/clients.js`; clients (non-admin) still go straight to their own workspace. |
| v1.31   | **Free tile movement + To-Do owner colours.** (1) Exec Summary columns are now **equal height** with a min-height and a "Drop a tile here" placeholder on empty columns, so tiles can always be dragged **back into any of the 3 columns** regardless of size (fixes not being able to move things into an emptied/short column). (2) To-Do owner tags: **TJA is always TJA orange**; the **Client** tag colour is **configurable** via a colour picker in the To-Do's header (stored per engagement as `todoClientColor`). |
| v1.30   | **Centered North Star + service-line sliders.** (1) The North Star banner content is now **centered**. (2) In admin view, each Service Line's gradient allocation bar is a **draggable slider** (0–100%) — drag to adjust the allocation; it updates live and persists. Clients still see the static gradient bar + %. |
| v1.29   | **Resizable tiles + North Star banner.** (1) Admins can **resize each Exec Summary tile in both directions** — drag the bottom-right corner; the size persists per engagement (alongside column widths). (2) The **North Star** is now its own **slim full-width banner** across all three columns at the top of the Executive Summary (moved out of the top bar, which now shows just client name + engagement). Project due-date rides along in the banner. |
| v1.28   | **Service-line states + PR metric.** Each Service Line now has a **3-state control — Not started / In progress / Complete** (segmented buttons, admin sets directly; clients see a read-only badge). New service lines default to "Not started". **PR Coverage** drops the ad-value-equivalent column — it now shows **estimated impressions** only. |
| v1.27   | **Exec Summary layout overhaul.** (1) Burn **speedometer % is now TJA orange** (was red/yellow/green). (2) Removed the admin disclaimer banner and the big client/North-Star header tile — **client name + engagement + North Star now live in the top bar** (North Star & project due-date still inline-editable there). (3) **Engagement Condition folded into the Burn tile** (no longer its own card). (4) Exec Summary is now a **3-column kanban with drag-to-resize columns** (widths persist per engagement); tile drag/remove/restore updated for 3 columns. (5) Tightened paddings/gaps so the summary fits with less scrolling. (6) **Backlog items now have an "est. hrs" (estimated hours to complete) field.** (7) **Monthly history is retained** — editing burn keeps the month-history (`mom`) in sync, and a **＋ New month** button banks the current month and rolls into the next, so past months are never lost (persists to Supabase). |
| v1.26   | **Monthly Services / Projects IA + project folder.** Renamed the top toggle to **Monthly Services ⇄ Projects** and **removed the Projects item from the left nav**. Projects is now a top-mode whose landing is a **folder of project tiles** (status dot + % progress) when there are multiple; clicking a tile opens that project's **Executive Summary** (Status / Present Docs / Files tabs work for it), with a **← All projects** link to return. One project opens straight to it. Added an **＋ New Project** button and a **two-step Delete** (✕ → explicit confirm) to prevent accidental deletes. Backlog stays Monthly-Services-only. Added **`?v=` cache-busting** to all local CSS/JS so live clients always get fresh assets after a deploy. (The detailed Project Plan tables are no longer surfaced in nav — code + data preserved for easy re-add.) |
| v1.25   | **Brand gradient + Retainer/Project restructure.** (1) Applied the official TJA gradient (`#EFAE41 → #EC9C39 → #DA662A`, as `--tja-grad`) to the retainer **speedometer** arc, the project **pizza tracker** (done dots + connectors), and **every progress bar** (service lines, project tiles, plan tasks). (2) Top engagement toggle is now a clean **Retainer ⇄ Project** switch instead of listing one chip per project. (3) The renamed **Projects** tab (project-only) opens to a **tile/folder grid** when there are multiple projects (click a tile to open it, ← All projects to go back) and **straight to the project** when there's only one; Backlog stays retainer-only. |
| v1.24   | **Real TJA logo wired in.** Replaced the drawn SVG placeholder with the official vector logo (`assets/img/tja-logo.svg`, brand `#f68e21` + white) in the sidebar and login card. The `<img>` now points at the SVG; the inline-SVG stays as the underneath fallback. |
| v1.23   | **Brand logo in the chrome.** Replaced the "THE JAMES AGENCY." text wordmark in the sidebar and on the login card with the TJA `tja` logo. Uses the real logo PNG (`assets/img/tja-logo.png`) layered over an always-on inline-SVG fallback, so the mark renders correctly even before/without the file. |
| v1.0    | Initial sandbox: login + 6 pages (Burn, SAP, Status, Key Dates, Docs, Files) with Celtic Elevator placeholder data and the burn dial gauge. |
| v1.1    | Present Docs rebuilt as an interactive creative-review tool: upload image creatives, click to enlarge, draw markup on the image (color pen, undo, clear), pick a status (Approve / Approve w/ changes / Revisions needed), and leave comments. Images downscaled + stored in localStorage. Removed placeholder doc cards. |
| v1.2    | Present Docs deliverables + versions: each tile is a deliverable holding V1/V2/V3… (resubmit adds a version; memory-log chips switch between them). Added a tool switch (Draw / Comment), true stroke-by-stroke Undo, pinned comments (click image to drop a numbered note), fit-to-screen image (no scrolling), and inline tile rename. Migrates v1.1 uploads automatically. |
| v1.3    | Fixed image fit (now `object-fit: contain` against a proper height cascade — the whole image always shows, overlay sized to the displayed picture rect, not natural pixels). Unified Undo now reverts pen strokes, Clear, AND comment-pin add/delete (button always visible + ⌘/Ctrl+Z). |
| v1.4    | Redesigned the comments side panel into proper comment cards: header with live count + **Clear all** (undoable), per-comment **resolve** (✓ dims the card + turns the pin green) and **delete**, and two-way highlight — click a comment to pulse its pin on the image and vice-versa. |
| v1.5    | **Admin vs Client modes.** Role tied to login (admin = full edit, `celticelevator@` = read-only data + can upload Files & review Present Docs). Admin-only **Mode switch** previews the client view without logging out (hidden from clients). Files tab now has a working **Upload File** (both roles, attributed by source; admin can remove uploads). Present Docs upload/new-version/rename/delete are admin-only. |
| v1.6    | Renamed the admin account to `clientservices@thejamesagency.com` (password `admin`). |
| v1.22   | **Supabase integration (gated).** Wired the portal to Supabase for a live, shared, multi-user product: real **Auth**, and the three data scopes (dashboard / files / deliverables) read/written to an `app_state` JSONB table with **Row-Level Security**. New files: `supabase/schema.sql`, `assets/js/supabase-config.js`, `assets/js/supabase-sync.js`, `SUPABASE_SETUP.md`. **Fully backward-compatible:** with blank config the app runs on localStorage exactly as before; pasting your project URL + anon key flips it to Supabase automatically (auth + shared data, localStorage as cache). See `SUPABASE_SETUP.md` for the 5-step go-live. |
| v1.21   | **Bug fixes: kanban drag + empty client view.** Tile drag was firing twice (tile + its column both handled the drop), sending tiles to the column end instead of the drop position — fixed with `stopPropagation`; drop-on-self is now a no-op and placement is precise. Added a **layout safety net**: de-dupes/validates module keys and, if both columns ever end up empty, restores the default — which fixes the **"client view empty"** regression (caused by a corrupted layout saved by the old drag bug). |
| v1.20   | **Project Plan + Status polish.** The **Status** tab is now editable like the rest (inline fields, click-to-cycle status, add/remove efforts & service-line groups) and shows a **completion count per service line** ("1/2 complete") — it fills the page instead of sitting as a bare read-only table. Added matching **section icons** (flag / list / risk) to the Project Plan headings for visual consistency. Both tabs use the full width. |
| v1.19   | Three fixes: (1) removed the 1180px content cap so the dashboard **uses the full width** — the right-side space is no longer locked off; (2) **milestones** redesigned — the tiny ✓/✕ replaced with a large **clickable check-circle** (fills green + strikethrough when done) and a hover-reveal delete; (3) the project **pizza tracker is now sequential** — clicking an undone phase fills up to it, clicking a done phase clears it *and everything after* (un-selecting phase 3 also un-ticks 4+). |
| v1.18   | **Drag tiles + draggable speedometer.** Replaced the kanban arrow buttons with **drag-and-drop** — grab a tile by its **header** to rearrange it within or across columns (subtle ✕ on hover removes a tile). The retainer **speedometer is now interactive**: drag the needle to set burn, or edit the **% directly** (used hours are derived from % of the contract total — no more typing raw burn hours). Verified live: header-drag reorder + cross-column move + persistence, and gauge drag/%-edit. |
| v1.17   | **Deep visual review + layout.** Fixed visual bugs found on review: the always-on orange **edit boxes** were noisy (every field boxed) → now subtle, revealing on hover / clear on focus, and empty fields show a faint dash instead of an orange pill; **orange text was low-contrast on white** → added a darker `--accent-text` for text-on-light (links, owner tags, PR outlets, KPI values). Restructured the Executive Summary into a **2-column kanban**: **PR Coverage now lives in the right column** (compact: outlet/date · headline · impressions/ad-value) instead of a full-width scroll-down, and admins can **move (↑↓), send across columns (←→), and remove/restore tiles** — layout persists per engagement. |
| v1.16   | **QA sweep + bug fix.** Exercised every control across all tabs, both themes, and both roles (nav, engagement toggle, theme, role/preview, undo, all Exec Summary edits + cross-links, Project Plan folders/New+Delete/cycling/add-remove/edits, Present Docs, Files, Backlog, login gate, sign-out, auth guard). **Fixed a real bug:** the Present Docs markup overlay sized to 0×0 on modal open (single `requestAnimationFrame` fired before layout), so drawing + comment pins silently failed — now retries until the image is decoded & laid out, and handles cached images. Everything else passed. |
| v1.15   | **Brand standards (brand-standards-V2).** Adopted the official palette — TJA Orange `#F68E21` (subtle accent), TJA Gray `#5F6165`, data-viz teal/blue/cool-gray tokens — over a black/white base. Typography now follows the guide: **headlines in Inter Black, ALL CAPS, 0 tracking**; body in **Inter Light** with light tracking (loaded weights 300/900). Added **custom line icons** to every Executive Summary module header and swapped the North Star ★ for the brand **bolt** motif. |
| v1.14   | **Editorial polish pass.** Light mode is now the **default** (matches thejamesagency.com first impression; still toggleable + persisted). Added subtle **card depth** (shadows) in light mode, more **whitespace** (roomier padding/gaps), and refined **typography** (larger, tighter headings; softer eyebrow labels). The Present Docs review modal now stays **dark in both themes** (ideal for reviewing imagery) via scoped token overrides. |
| v1.13   | **Visual system + light/dark mode.** Refactored colors into themeable tokens; added a **light mode** (editorial/airy, inspired by thejamesagency.com) alongside the refined dark mode, toggled by a topbar sun/moon button (persisted, flash-free on load). Introduced a brand **gradient** (orange→teal) on primary CTAs "where it makes sense," kept orange as the primary accent. Gauge + surfaces made theme-neutral for readability in both modes. |
| v1.12   | **Undo (admin)** — every dashboard-data edit funnels through one undo stack; an **↶ Undo** button in the topbar (with a count) plus **⌘Z / Ctrl+Z** revert the last action (admin only; native text-undo still works inside a field). Retainer **burn is now clearly editable** (the "X of Y hrs used" numbers are inline-editable and update the gauge live). Added a **migration** so older saved data (pre-`projects[]`) upgrades automatically — fixes the "Project disappeared" issue caused by stale localStorage. |
| v1.11   | **Multiple projects per client** + **editable Project Plan.** Each client now holds an array of projects (Website Redesign, Brand Refresh…); the top toggle lists Retainer + one chip per project, and the Project Plan tab has a **project folder bar** to switch between them. Admins can edit the whole plan inline — click R/Y/G dots and status badges to cycle, edit any field, ＋ add milestones/phases/tasks/risks, rename/delete a project. **＋ New Project** spins up a plan pre-seeded with a Discovery→Launch phase skeleton, and Status shows a live "tasks avg %" roll-up hint. All admin-only; client view stays read-only. |
| v1.10   | Ingested the two reference Google Sheets and built **Step 4** to the real formats. **Project Plan** tab rebuilt: Outcome + R/Y/G status header, a **Key Milestones / critical-path** table (R/Y/G · item · owner · target · why · action), a phased **Detailed Plan** task list (# · task · who · window · % progress · status · notes), and a **Risks & Watch Items** table. **PR Coverage** module now matches the real tracker columns — Date · Outlet · Headline · Impressions · Ad Value. (The two Sheets turned out to be a Project Plan and a PR Coverage report, not Status/SAP — the SAP+Status merge is still being defined by the team.) |
| v1.9    | Real **Celtic Elevator logo** (white background removed via corner flood-fill → transparent PNG at `assets/img/celtic-elevator-logo.png`) now replaces the "CE" orange box in the topbar avatar and the Executive Summary header. Driven by `client.logo` in the data (falls back to initials for clients without a logo). |
| v1.8    | Executive Summary hardening pass: fixed the scroll/layout (sidebar + topbar now pin cleanly while `.main` scrolls — no more black gap), balanced the two-column rows (`align-items: start`, condition card no longer half-empty), and added admin edit affordances — hover states + tooltips on the condition dots, pizza phases, service-line status, owner tags, and MoM chips so it's obvious what's clickable. |
| v1.7    | **V1 restructure (06/16 workshop).** New **Executive Summary** homepage with modules: burn (speedometer for retainers / pizza tracker for projects, manual — projected depletion removed), R/Y/G condition, service lines + month-over-month, milestones timeline, To-Do's, Dependencies, KPIs, scrolling PR coverage. **Retainer/Project engagement toggle.** Every homepage field is admin-editable inline (persisted to localStorage) and read-only in client view. Tabs reorganized to Executive Summary · Project Plan · Status · Present Docs · Files · Backlog (retainer-only). |

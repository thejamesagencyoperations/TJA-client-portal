/* ============================================================
   SUPABASE CONFIG
   Paste your project's URL + anon (public) key below, then the
   portal goes live on Supabase. Find them in:
   Supabase → Project Settings → API.

   Leave BOTH blank to keep running on local mock/localStorage data
   (the offline sandbox). Filling them in flips the app to Supabase
   for auth + shared data automatically.
   ============================================================ */
window.SUPABASE_CONFIG = {
  url: "",      // e.g. "https://abcdefgh.supabase.co"
  anonKey: "",  // the "anon public" key (safe to expose in the browser)
};
